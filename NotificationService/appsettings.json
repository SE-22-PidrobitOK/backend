{
  "ConnectionStrings": {
    "DefaultConnection": "Server=${SQL_SERVER};Database=${NOTIFICATION_DATABASE};User Id=${SQL_USER};Password=${SQL_PASSWORD};TrustServerCertificate=True"
  },
  "Jwt": {
    "Secret": "${JWT_SECRET}",
    "Issuer": "Pidrobitok.NotificationService",
    "Audience": "Pidrobitok.Frontend",
    "TokenLifeTimeInMinutes": "${JWT_TOKEN_LIFETIME}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}